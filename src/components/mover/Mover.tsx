import React from 'react';

const Mover = () => {
  return (
    <div>
      <div>
        <div>X</div>
      </div>
      <div>
        <button>move to top left</button>
        <button>move to top right</button>
        <button>move to center</button>
        <button>move to bottom left</button>
        <button>move to bottom right</button>
      </div>
    </div>
  );
};

export default Mover;